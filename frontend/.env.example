# Backend base URL for the frontend. Leave unset/empty to call the same origin
# ("/api" — use when the API is on the SAME Vercel deployment).
# Set it to point the Vercel frontend at an external backend:
#   VITE_API_BASE_URL=https://invest-search-api.onrender.com/api
# VITE_API_BASE_URL=
